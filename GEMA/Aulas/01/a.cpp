#include <algorithm>
#include <bits/stdc++.h>
using namespace std;

// binary search
int main() {

  auto it =
      lower_bound(ForwardIterator first, ForwardIterator last, const Tp &val);

  int id = it - v.begin();
  // procura rpimeiro elemento maior ou igual
  auto it = upper_bound(FIter, FIter, const Tp &)
}
